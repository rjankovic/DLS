var stFlowActions = (function () {
    /*
    var rootSelection = function (container, data, selectedCallback) {
        $(container).jstree(
            {
                'core': {
                    'data': data
                }
            }
        );

        $(container).on("changed.jstree", function (e, data) {
            selectedCallback(data.selected);
        });
    }
    */
    return {
    }
})();


